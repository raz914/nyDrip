import BlogAdminSection from "@/components/admin/BlogAdminSection";

export const metadata = {
  title: "Blog Admin | DripLounge",
  robots: "noindex, nofollow",
};

export default function Page() {
  return <BlogAdminSection />;
}
