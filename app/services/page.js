import ServicesPage from "@/components/services/ServicesPage";

export const metadata = {
  title: "Wellness Services | DripLounge",
  description:
    "Explore IV therapy, peptide wellness, injections, TRT, memberships, and concierge wellness services from DripLounge.",
};

export default function Page() {
  return <ServicesPage />;
}
