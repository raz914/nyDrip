import CompressionBootsPage from "@/components/services/compression-boots/CompressionBootsPage";

export const metadata = {
  title: "Compression Boots | DripLounge",
  description:
    "Explore Compression Boots benefits, pricing, and recovery session booking at DripLounge.",
};

export default function Page() {
  return <CompressionBootsPage />;
}
